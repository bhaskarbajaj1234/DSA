#Code:

class Solution {
    public int getLength(ListNode head) {
        // Your code goes here
        if(head == null){
            return 0;
        }
        ListNode temp = head;
        int len = 1;
        while(temp.next != null){
            len += 1;
            temp = temp.next;
        }
        return len;
    }
}
